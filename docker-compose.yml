services:
  karate-tests:
    image: karatetest
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./target:/usr/src/app/target
      - ~/.m2:/root/.m2
    command: ["mvn", "clean", "test", "-Dkarate.env=prod", "-Dmaven.clean.failOnError=false"]